summary: Create a Bluesky post (text, OGP link post, or manual image post) without creating a Skyshare entry
operationId: createBskyRecord
parameters:
  - name: cookie
    in: header
    required: true
    schema:
      $ref: ../../../common.yaml#/cookie
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        anyOf:
          - required:
              - text
          - required:
              - ogMeta
              - ogImage
          - required:
              - images
              - imagesMeta
        properties:
          text:
            type: string
            example: Post content
            minLength: 1
          ogImage:
            type: string
            format: binary
          ogMeta:
            $ref: ../../../common.yaml#/ogMeta
          images:
            type: array
            description: >-
              手動画像添付モード（skyshare entry を作成せず Bluesky にのみ画像を添付する）用の画像。
              指定する場合は `imagesMeta` を併せて必須とする。
            items:
              type: string
              format: binary
          imagesMeta:
            $ref: ../../../common.yaml#/imagesMeta
          langs:
            type: array
            items:
              type: string
              example: ja
          selfLabels:
            type: string
            enum:
              - sexual
              - nudity
              - porn
              - spoiler
              - "!warn"
responses:
  "200":
    description: Success post
    content:
      application/json:
        schema:
          type: object
          properties:
            url:
              type: string
              example: https://bsky.app/profile/alice.bsky.social/post/3lxyz
            uri:
              type: string
              example: at://did:plc:abc/app.bsky.feed.post/3lxyz
            cid:
              type: string
          required:
            - url
            - uri
            - cid
  "400":
    description: Bad Request
    content:
      application/json:
        schema:
          $ref: ../../../common.yaml#/error
  "401":
    description: Unauthorized
    content:
      application/json:
        schema:
          $ref: ../../../common.yaml#/error
  "429":
    description: Too Many Requests
    content:
      application/json:
        schema:
          $ref: ../../../common.yaml#/error
  "500":
    description: Internal Server Error
    content:
      application/json:
        schema:
          $ref: ../../../common.yaml#/error
