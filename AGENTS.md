# 重要な注意点

本システムは Cloudflare Pages / Workers 環境で動作するため、Node.js 固有の API は使用できない。

# ディレクトリ構成

- 目的: 新しいフロントエンド実装（Astro ベース）。
- legacy/: 旧フロントエンド実装（React + Firebase Functions）。
  - v2バックエンドを利用し、表面上のUIのみを提供する。v2バックエンドの機能を利用するため、legacyフロントエンドは将来的に廃止される予定。
- public/: 静的アセット（画像、マニフェスト等）
- hack/: 自動化スクリプト等を配置
- src/: アプリ本体。主なサブディレクトリを指す。
  - `client/`: 各APIの client。自動生成であるため、配下のファイルを編集しないこと。
    - `atproto/`: lexicons定義からの自動生成であるため編集しないでください。
    - `openapi/`: OpenAPI定義からの自動生成であるため編集しないでください。
  - `lib/`: 共通ユーティリティ・再利用コード。
  - `components/`: Reactコンポーネント。AstroフロントエンドはReactコンポーネントで作成する。
  - `pages/`: ページルートおよびコンテンツ（Astroページ）。
    - `api/`: フロントエンド用APIエンドポイント。
  - `schema/`: フロントエンド用APIエンドポイントのリクエスト・レスポンススキーマを定義。
- lexicons/: atproto lexicons 定義。
- openapi/: OpenAPI 定義。
- **注意**: 旧フロントエンドは `astro/` に残存しているため、現在の開発・変更は `frontend/` を基準に行ってください。

# Astroサーバサイド実装

`src/pages/v1` 配下のファイルは Cloudflare Workers 環境で動作するサーバーサイドコードです。Node.js 固有の API は使用できません。

また、このAPIは openapi/ 配下の OpenAPI 定義に基づいてクライアントを生成してください。src/pages/src/pages/v1 のサーバ実装はOpenAPIクライアントに合わせて入力と出力を合わせること。

サーバ向けスキーマは `npm run apigen`により `src/client/openapi/schemas`配下に生成される。

# Reactコンポーネント

AstroフロントエンドはReactコンポーネントで作成する。

- コンポーネントは必ずアロー式で示し、デフォルトエクスポートを行うこと。
- コンポーネントは src/components 直下に、コンポーネントごとにディレクトリを作成すること。また、コンポーネントを構成する要素はindex.tsxとindex.module.cssとすること。サブディレクトリは作成しないこと。
  内部からAstroサーバサイドにリクエストを送る場合、`src/client/openapi/client`を使うこと
  ex:

```ts
export const Component = () => {
	return ...
}
export default Component
```

# CSS規定

- tokens.css では、最低限必要なテーマカラーやサイズ規定を記載している。
- ボタンなど、ユーザの操作にあたるインターフェースについては、ui.module.cssに定義されたスタイルを指定して利用すること。また、コンポーネントは各クラスを組み合わせて使用すること。
- デバッグ時に混乱をきたすため、すべてのプロパティは規定を用い、フォールバックによる直指定は避けること。

# コード理解負債改善規定

## 適用範囲

- 対象は `src/` 配下の手書きコードとする。
- 自動生成コードは対象外とする。
  - `src/client/atproto/`
  - `src/client/openapi/`

## 必須ルール

- ファイル先頭に、そのファイルの責務と処理概要を記載すること。
- 関数・メソッドごとに、少なくとも以下をコメントで明示すること。
  - 何をする処理か（目的）
  - 入力（Input）
  - 出力（Output）
  - 使用例（Example）
- `unknown` や外部入力を扱う処理では、期待する入力形状（最小要件）を明示すること。
- 分岐やループが複雑なブロック（目安: ネスト2段以上、または条件分岐3つ以上）には、
  「何を検証・除外・抽出しているか」の意図コメントを記載すること。

## 推奨ルール

- 失敗時の方針（`undefined` を返す、早期 return する、など）をコメントに含めること。
- 副作用を伴う処理（状態更新、外部I/O、リソース解放など）は理由をコメントで補足すること。
- 実装上の制約（Cloudflare Workers制約、API互換性要件など）を必要に応じて明記すること。

## 禁止ルール

- コードを見れば自明なコメント（例: 代入、単純インクリメントの説明）は記載しないこと。
- 実装と乖離した古いコメントを放置しないこと。
- 日本語コメントを基本とし、同一責務の説明で日英を混在させないこと。

## 関数コメントの最小テンプレート

```ts
/**
 * 機能の要約（1-2行）
 *
 * 想定する入力形状(必要な場合):
 * - xxx
 *
 * 処理の趣旨:
 * - xxx
 *
 * Input:
 * - `arg`: 説明
 *
 * Output:
 * - 戻り値の説明
 *
 * 例:
 * - 入力: ...
 * - 出力: ...
 */
```

## 導入手順

- 新規実装コードには本規定を即時適用すること。
- 既存コードは段階的に適用すること。
  1.  `src/lib/` の高複雑度ファイルから着手
  2.  `src/components/` の非同期・分岐が多いコンポーネントへ拡大
- 変更時は、コメント追加のみでなく内容の正確性を必ずレビューすること。

## PRレビュー観点

- ファイル先頭に責務・概要があるか。
- 各関数に Input/Output/例があるか。
- 複雑ブロックに意図コメントがあるか。
- 自動生成コードを編集していないか。
