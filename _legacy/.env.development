PUBLIC_BACKEND_ENDPOINT="http://192.168.3.151:5001/skyshare-v1/asia-northeast1/firebaseAPI/api/v1"
# ローカルでv2の開発サーバー(例: npm run dev, 別ポート)を別途起動し、そのオリジンを指定する。
# v2側はCORS未対応(同一オリジン配信が前提)のため、別ポートで動かす場合は
# リバースプロキシ等で同一オリジンにまとめない限りログイン/投稿の疎通は失敗する。
PUBLIC_V2_BACKEND_ENDPOINT="http://localhost:4321"
