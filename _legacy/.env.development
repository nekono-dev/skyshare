PUBLIC_BACKEND_ENDPOINT="http://192.168.3.151:5001/skyshare-v1/asia-northeast1/firebaseAPI/api/v1"
# v2(4321)の開発サーバーへは、legacy dev サーバー自身のvite proxy(astro.config.mjs)
# 経由で /v1/* のみをサーバー間通信として転送する。ブラウザからは同一オリジン
# (localhost:4322)への相対パスとして疎通するため、CORSやCookie共有の問題を回避できる。
PUBLIC_V2_BACKEND_ENDPOINT=""
